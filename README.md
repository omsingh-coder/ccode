# Single-folder Multiplayer Chess (Secrets)

Mobile-first multiplayer chess demo where each player submits a secret before game start; the winner alone sees the opponent's secret at the end.

## Run locally

1. Node >= 16 installed.
2. Set environment variable MASTER_KEY (32 byte string). Example (Linux/macOS):
